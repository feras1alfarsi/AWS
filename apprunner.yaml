version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install streamlit boto3 requests
run:
  command: python -m streamlit run app.py --server.port 8080 --server.address 0.0.0.0 --server.enableCORS false
  network:
    port: 8080